#!/bin/sh

set -eu

cd app; npm run build; cd ..

IMAGE_NAME=firebase

docker run -it \
  -v $PWD/.firebase_config:/root/.config \
  -v $PWD:/app \
  -w /app \
  $IMAGE_NAME firebase deploy --only functions,hosting
